#include <stdio.h>
#include <stdlib.h>

int main()
{
    //IF ELSE TUSHUNCHASI

    int a;
    printf("son kiriting:");
    scanf("%d",&a);


    if (a>0) {
        printf("bu son musbat");
            }

    else if (a<0) {
        printf("bu son manfiy");
                 }
    else {
        printf("siz 0 kiritdingiz");
        }
    return 0;
}
